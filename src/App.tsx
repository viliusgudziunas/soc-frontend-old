import { HomePage } from 'pages';
import { ReactElement } from 'react';

const App = (): ReactElement => (
  <div className='font-mono container flex justify-center mx-auto'>
    <HomePage />
  </div>
);

export default App;
